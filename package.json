{
  "name": "validate-token",
  "version": "4.0.0",
  "description": "Validate OIDC tokens in Cognigy.AI Flows",
  "main": "build/module.js",
  "scripts": {
    "transpile": "tsc -p .",
    "zip": "tar cfz validate-token.tar.gz build/* package.json package-lock.json README.md icon.png",
    "build": "npm run transpile && npm run lint && npm run zip",
    "lint": "tslint -c tslint.json src/**/*.ts"
  },
  "keywords": [
    "Token Validation",
    "OIDC",
    "JWT"
  ],
  "author": "Cognigy GmbH",
  "license": "MIT",
  "dependencies": {
    "@cognigy/extension-tools": "^0.12.0",
    "jose": "^6.2.3",
    "tslint": "^6.1.2"
  },
  "devDependencies": {
    "@types/node": "^13.13.15",
    "typescript": "^6.0.3"
  }
}
